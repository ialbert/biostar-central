language: python

python:
  - "2.7"
  #- "3.3"

addons:
  postgresql: "9.4"

env:
  DATABASE_NAME=travis_ci_test

before_script:
  - createuser -d biostar
  - createdb $DATABASE_NAME -E utf8 --template template0

install:
    - pip install -q -r conf/pip/travis.txt

script: source conf/travis.env && python manage.py test

