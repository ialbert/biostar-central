


def search(reqeust):


    print(reqeust.GET)
    1/0

    # def populate_dict(query_list, query_dict):
    #     key, val = query_list.split("=")
    #     try:
    #         val = float(val)
    #     except Exception as exc:
    #         pass
    #     query_dict[key] = val
    #
    # args = parse.parse_args()
    # query_list = list(map(str.strip, args.output.split(",")))
    #
    # query_dict = dict()
    # func = partial(populate_dict, query_dict=query_dict)
    # list(map(func, query_list))
    #
    # print(Measurement.objects.filter(**query_dict))

    return