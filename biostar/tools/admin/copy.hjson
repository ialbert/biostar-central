#
# Copy data from source to target
#
source : {
    value: 1
  }

target : {
    value: 2
  }

settings : {
    name: Copy data
    help: Copy data from source to target.
    summary: Copy data from source to target.
    }
