#
# Admin job that unpacks files in the background.
#
target : {
    value: 2
  }

settings : {
    id:unpack
    name: Unpack data
    help: Admin job that unpacks files in the background.
    summary: Admin job that unpacks files in the background.
    }
